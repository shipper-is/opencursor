{
  "name": "open-cursor",
  "displayName": "Open Cursor",
  "description": "Use any supported AI provider in Cursor through one guided setup",
  "version": "0.5.4",
  "publisher": "shipper-is",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/shipper-is/opencursor.git"
  },
  "bugs": {
    "url": "https://github.com/shipper-is/opencursor/issues"
  },
  "homepage": "https://github.com/shipper-is/opencursor#readme",
  "engines": {
    "vscode": "^1.90.0",
    "node": ">=20"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "cursor",
    "models",
    "openai",
    "api",
    "proxy"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "openCursorModels.openSetup",
        "title": "Open Settings",
        "category": "Open Cursor",
        "icon": "$(settings-gear)"
      },
      {
        "command": "openCursorModels.showLogs",
        "title": "Show Request Log",
        "category": "Open Cursor"
      }
    ],
    "configuration": {
      "title": "Open Cursor Models",
      "properties": {
        "openCursorModels.proxyPort": {
          "type": "number",
          "default": 18420,
          "minimum": 1024,
          "maximum": 65535,
          "description": "Port for the local OpenAI-compatible routing proxy."
        },
        "openCursorModels.modelPrefix": {
          "type": "string",
          "default": "oc-",
          "description": "Prefix added to custom model names in Cursor to avoid conflicts with built-in models."
        },
        "openCursorModels.autoStartProxy": {
          "type": "boolean",
          "default": true,
          "description": "After initial setup, start the proxy automatically when Cursor launches."
        },
        "openCursorModels.logRequests": {
          "type": "boolean",
          "default": false,
          "description": "Log proxied request paths and routes to the Open Cursor Models output channel."
        },
        "openCursorModels.logRequestBodies": {
          "type": "boolean",
          "default": false,
          "description": "Log each proxied request's detected agent role, tool names, turn count, and a system-prompt preview. Useful for verifying subagent detection; may include prompt text in the output channel."
        },
        "openCursorModels.forceSubagentModel": {
          "type": "boolean",
          "default": false,
          "description": "Route requests detected as subagent (Task tool) runs to a fixed model, ignoring the model the orchestrating agent selected. Detection is heuristic — verify with logRequestBodies first."
        },
        "openCursorModels.subagentModel": {
          "type": "string",
          "default": "",
          "description": "Model used for subagent runs when forceSubagentModel is enabled. Accepts a profile display name, slug, or the full prefixed Cursor model name, matched case-insensitively. Must name one of your own models — Cursor's built-in models cannot be targeted, because the proxy has no way to hand a request back to Cursor's own inference."
        },
        "openCursorModels.subagentReasoningEffort": {
          "type": "string",
          "enum": [
            "inherit",
            "default",
            "none",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ],
          "default": "inherit",
          "description": "Thinking/reasoning depth for subagent runs. \"inherit\" uses the subagent model profile's own setting."
        },
        "openCursorModels.subagentSpeedTier": {
          "type": "string",
          "enum": [
            "inherit",
            "default",
            "fast",
            "economy"
          ],
          "default": "inherit",
          "description": "Capacity tier for subagent runs. \"inherit\" uses the subagent model profile's own setting."
        },
        "openCursorModels.tunnelProvider": {
          "type": "string",
          "enum": [
            "auto",
            "ngrok",
            "cloudflared"
          ],
          "default": "auto",
          "description": "Tunnel provider. Auto tries ngrok, then cloudflared."
        },
        "openCursorModels.providerSlot": {
          "type": "string",
          "enum": [
            "openai",
            "anthropic",
            "google"
          ],
          "enumDescriptions": [
            "Use Cursor's OpenAI API Key field and oc- model names.",
            "Use Cursor's Anthropic API Key field and claude-oc- model names.",
            "Recommended. Use Cursor's Google API Key field and gemini-oc- model names."
          ],
          "default": "google",
          "description": "Which Cursor API key field carries the generated OpenCursor proxy key."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node --import tsx --test test/**/*.test.ts",
    "package": "vsce package",
    "vsix:path": "node -p \"var p=require('./package.json'); p.name + '-' + p.version + '.vsix'\""
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^5.7.0"
  }
}
